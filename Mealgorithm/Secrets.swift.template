// Secrets.swift.template
// Copy this file to Secrets.swift and replace the placeholder with your actual API key.
// Secrets.swift is gitignored — never commit it.

enum Secrets {
    static let spoonacularAPIKey = "YOUR_SPOONACULAR_API_KEY_HERE"
}
